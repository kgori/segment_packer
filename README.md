Segment Packer
==

Packs segments into layers, without overlaps.

To build:

``` bash
cmake -B build -S .
cmake --build build
cmake --build build --target r_package # to build and install an R package
```
